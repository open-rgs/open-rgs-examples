{
  "name": "@open-rgs-examples/play",
  "version": "0.1.0",
  "private": true,
  "description": "Boot a game in-process and drive one real round through the WebSocket client — watch the wire protocol and the wallet log happen.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@open-rgs/core": "^1.0.0",
    "@open-rgs/contract": "^1.0.0",
    "@open-rgs/client": "^1.0.0",
    "@open-rgs-examples/mock-platform": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
