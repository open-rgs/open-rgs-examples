{
  "name": "@open-rgs-examples/autotune",
  "version": "0.1.0",
  "private": true,
  "description": "RTP autotuner — measure a game's RTP and compute the paytable scale factor to hit a target. The hand-work, automated.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@open-rgs/contract": "^1.0.0",
    "@open-rgs/simulator": "^1.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
