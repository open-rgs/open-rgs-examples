{
  "name": "@open-rgs-examples/slots-classic",
  "version": "1.0.0",
  "private": true,
  "description": "Classic 5×3 video slot with scatter-triggered free spins, resolved atomically inside the trigger spin. The 'safe by construction' slot.",
  "type": "module",
  "scripts": {
    "dev": "bun src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@open-rgs/core": "^1.0.0",
    "@open-rgs/contract": "^1.0.0",
    "@open-rgs-examples/mock-platform": "workspace:*",
    "@open-rgs-examples/lua-kit": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
